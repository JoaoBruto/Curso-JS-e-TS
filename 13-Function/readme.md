# Funções — Anotações rápidas ✅

- **Geralmente usamos funções esperando que retornem algo.**
- **Funções seguem as mesmas regras que variáveis**, como escopo e atribuição.

## Exemplo: Função básica de soma 🔧

```js
function soma(x, y) {
    const resultado = x + y
    return resultado
}
```

Uso:

```js
resultado(soma(2,2))
```

Saída esperada:

```
Resultado = 4
```

> Dica: verifique sempre o `return` para garantir que a função retorne o valor desejado. 💡